version: 2

# Native-process MCP server (macOS-local). No Dockerfile, so no docker ecosystem.
# uv ecosystem updates both pyproject.toml AND uv.lock in a single PR.

updates:
  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "fix"
      prefix-development: "chore"
      include: "scope"
    labels:
      - "dependencies"
    groups:
      things-py:
        patterns:
          - "things-py"
      python-runtime:
        patterns:
          - "fastmcp"
          - "pydantic-settings"
          - "httpx"
          - "wsproto"
      dev-tools:
        dependency-type: "development"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    commit-message:
      prefix: "ci"
      include: "scope"
    labels:
      - "dependencies"
      - "ci"
